// Use strict

// Internamente JavaScript lo que hace es elevar la variable y la declara
pi = 3.1416; // Solo asigna un valor a la variable
console.log(pi);

// Para utilizar el modo estricto usamos:

"use strict"; // Esto aumenta el cumplimiento de las reglas
u = 3.45;
console.log(u);